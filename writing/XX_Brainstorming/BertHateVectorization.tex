\documentclass{article}
\usepackage{graphicx}
\usepackage[]{algorithm2e}

\begin{document}

\title{Introduction to \LaTeX{}}
\author{Author's Name}

\maketitle


\section{Vectorisation of CSV file}

\begin{algorithm}[H]
 \KwData{cleaned CSV file}
 \KwResult{Tensor with BERT vectorised tweets and additional info as matrix}
 initialization\;
 %\For{row} in CSV
 \For{row in CSV}{
    1. Tokenisation
    "apply", "tokenisation",  "to",  text in row (FUNCTION)
    2. Tokens to numbers: (FUNCTION)
        a) Bert Vectorize (FUNCTION)
        b) Dictionary on tokens (FUNCTION)
        c. Concatinate vectors into matrix (first bert, than dictionary, or vice versa) (optional)
    3. Save Matrix & Tensor (labels) in two files
 }
 \caption{How to write algorithms}
\end{algorithm}


\section{Tokenisation}

\begin{algorithm}[H]
 \KwData{tweet as string}
 \KwResult{List of strings (tokens)}
    cuts tweet where spaces exist
\end{algorithm}


\section{Tokens to numbers}

\begin{algorithm}[H]
 \KwData{List of strings (tokens)}
 \KwResult{Matrix of numbers (vectorisation + enrichment}
    vector1 = Bert vectorisation
    vector2 = Hatespeech metric
    
    matrix = [vector1, vector2]
\end{algorithm}



\section{Bert Vectorize}

\begin{algorithm}[H]
 \KwData{List of strings (tokens)}
 \KwResult{Vector of numbers (one number per word)}
    NewList = []
    
    \For{word in input list}{
        NewList.append(Bert(word))
    }
\end{algorithm}

\section{Hatetermsearch}
\begin{algorithm}[H]
 \KwData{List of strings (tokens) \\
 ["Hallo","mein","Haus","ist","scheiße."]}
 % 
 \KwResult{Vector of numbers (one number per word)\\
 [0,0,0,0,46.5]}
 %
     NewList = []
    
    Lemmatize hatebase dictionary (keyterm column), eliminate duplicates
    \For{word in input list}{
        HatetermsearchOneWord(word)
        
        NewList.append(hatevalue)%hateterm yes (1) or no (0)
        
    }
\end{algorithm}


\section{HatetermsearchOneWord}
\begin{algorithm}[H]
 \KwData{Word (String \\
 "scheiße"}
 % 
 \KwResult{Number determining hatefulness\\
 46.5}
 %

   def DetermineHatefulness(word)
    
    Output 1 if hateterm, 0 if no hateterm
    if 1, get offensiveness_value
    if 1, check if unambiguous
        if unambiguous, multiply offensiveness_value by 2
    replace 1 by offensiveness_value (including unambiguity weighting)
        
    
\end{algorithm}
        
        
%read current\;
%  \eIf{understand}{
%   go to next section\;
%   current section becomes this one\;
%   }{
%   go back to the beginning of current section\;
%  }

\section{Vectorization}

\section{Conclusion}
Write your conclusion here.

\end{document}